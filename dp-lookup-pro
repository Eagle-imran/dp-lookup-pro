#!/usr/bin/env python3
"""
Thin CLI shim. All logic lives in cts_dp_lookup_pro.main().

The previous shebang was '#!/usr/bin/env uv run python'. macOS splits shebang
arguments, but Linux passes the whole remainder as a single argument, so that
form failed there with:  env: 'uv run python': No such file or directory
and could not run on Windows at all.

Portable invocations, in order of preference:
    uv run python dp-lookup-pro WORLI 947     # works everywhere, no install
    dp-lookup-pro WORLI 947                   # after: pip install -e .
    ./dp-lookup-pro WORLI 947                 # needs deps on the system python
"""
import sys

from cts_dp_lookup_pro import main

if __name__ == "__main__":
    sys.exit(main())
